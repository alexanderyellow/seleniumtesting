This is light version of the test automation framework.
For building and running tests may use Maven. In Run/Debug configurations need point out only working directory
and 'clean'(optional) 'test' commands.
In the resources/browserconfig.properties file you have to point out your system and webdriver settings.